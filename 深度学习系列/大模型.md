# Scaling Law

Scaling Law 是指模型的性能与 “计算量、模型参数量和数据大小” 三者之间存在的关系。增加计算量、模型参数量或数据大小都可能会提升模型的性能，但是提升的效果会随着这些因素的增加而递减。

[大模型中的Scaling Law是什么？-阿里云开发者社区](https://developer.aliyun.com/article/1446631)

## Parallel Scaling

Qwen 推出的新 Scaling Law ---- Parallel Scaling。

> 简洁

参数和推理时候的算力扩展早就证明了，计算资源越多，模型就越聪明。能不能整个新招儿来扩展算力呢?

提出「并行扩展」的方法。在参数量不变的情况下，同时拉大训练和推理并行计算量。进而扩展出一条新规律，还通过预训练验证了。一个带 P 条并行流的模型，效果差不多相当于把参数按$O(logP)$ 这么 scaling(扩展)，推理效率还更高！

> 动机 Moment

来源于 diffusion 模型必用的一个 trick。 

Classifier-Free Guidance（CFG）。CFG 在推理阶段拿到输入 x 时，首先做一次正常的 forward 得到 $f(x)$；然后再对 x 进行主动的劣化（比如去除条件）变为 $x'$，再进行一次 forward 得到 $f(x')$。最终的输出 $g(x)$ 是 $f(x)$ 和 $f(x')$ 的一个加权组合，它的效果比 $f(x)$ 更好，更能遵循输入的条件。

$f(x)$ 和训练阶段是对齐的，而 $g(x)$ 明显和训练阶段的目标存在 $gap$。按照常识，只有训练目标和推理目标形式相同，推理才能发挥最大效果。另外，$f(x)$ 的参数量和 $g(x)$ 也是相同的，输入的有效信息量也相同，为什么 $f(x)$ 反而学不到 $g(x)$ 的能力？

个人看法：这不就是半监督学习里或 TTA 增强里常用策略的改编吗？先前有半监督学习的论文讨论过更大的并行计算确实能提高分割的效果（一强增强一弱增强 VS 两强增强一弱增强）。

> 作者的看法

CFG 生效的原因，本质上是用了双倍的并行计算量，它拉大了模型本身的 capacity。并进行了进一步的 scale。

- 把输入的变换形式（比如人为设计的劣化规则）和输出的聚合规则变成可学习的。
- 拉大并行的数目

![image-20250605114732860](imgs\image-20250605114732860.png)

> Parallel Scaling Law

[(37 封私信 / 80 条消息) 如何看待Qwen推出的新Scaling Law ——Parallel Scaling？ - 知乎](https://www.zhihu.com/question/1907422978985169131/answer/1907565157103694086)